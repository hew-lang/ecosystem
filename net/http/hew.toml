[package]
name = "hew.net.http"
version = "0.3.0"
edition = "2026"
hew = ">=0.6.0-rc1, <0.7.0"
description = "Actor-owned HTTP/1.1 server with typed errors and one-request handling"
authors = ["Hew Contributors"]
license = "MIT OR Apache-2.0"
keywords = ["http", "http1", "server", "actor", "networking"]
repository = "https://github.com/hew-lang/ecosystem"

[native]
crate = "."
lib = "hew_hew_net_http"
kind = "staticlib"
